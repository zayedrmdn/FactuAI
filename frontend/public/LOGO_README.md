# Logo Placeholder

The landing page references `/factuai-logo.png` for the logo.

You can either:

1. Add a `factuai-logo.png` file to the `public` folder, or
2. Update the image references in the components to use a different logo path

For now, the landing page will work without the logo image (it will show a broken image icon), but all other functionality will work perfectly.
